import React from 'react'
import { View, Text, StyleSheet } from 'react-native'

const Loading = ({ text }) => {
  return (
    <View>
      <Text>{text}</Text>
    </View>
  )
}

export default Loading
