import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  inputForm: {
    width: '100%',
    marginTop: 20
  },
  iconRight: {
    color: '#c1c1c1'
  }
})

export default styles
