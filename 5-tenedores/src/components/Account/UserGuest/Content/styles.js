import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  image: {
    height: 300,
    width: '100%',
    marginBottom: 40,
    marginTop: 8
  },
  title: {
    fontWeight: 'bold',
    fontSize: 19,
    marginBottom: 10,
    textAlign: 'center'
  },
  description: {
    textAlign: 'center',
    marginBottom: 20
  }
})

export default styles
