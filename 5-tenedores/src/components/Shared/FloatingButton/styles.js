import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  containerIcon: {
    position: 'absolute',
    bottom: 10,
    right: 10,
    elevation: 2,
    borderRadius: 100,
    padding: 12
  }
})

export default styles
