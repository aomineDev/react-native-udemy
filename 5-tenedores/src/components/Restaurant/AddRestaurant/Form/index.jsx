import React from 'react'
import { StyleSheet, View, Text } from 'react-native'

export default function Form ({ toastRef, setIsLoaidng, navigation }) {
  return (
    <View>
      <Text>Add Restaurant Form</Text>
    </View>
  )
}

const styles = StyleSheet.create({

})
