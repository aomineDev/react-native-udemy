import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  viewForm: {
    marginRight: 10,
    marginLeft: 10
  },
  viewFormBtn: {
    marginLeft: 20
  }
})

export default styles
